# Entirely optional. The app defaults to Ollama on this machine and needs no config,
# and cloud keys can be pasted into the app's Settings panel instead.
#
# Use this file only if you'd rather keep keys out of the browser. Copy to .env.local.

# ANTHROPIC_API_KEY=sk-ant-...
# OPENROUTER_API_KEY=sk-or-...
